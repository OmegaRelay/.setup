[init] 
	defaultBranch=master
[alias]
	st = status
	br = branch
	co = checkout
	cm = commit
	cmm = "commit -m"
	r = reset
	rsh = "reset --hard"
	rst = "restore --staged"
[include]
	path = ~/.gitconfig-local
